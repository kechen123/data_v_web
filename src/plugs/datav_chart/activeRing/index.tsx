import { ActiveRingChart } from '@jiaminghi/data-view-react'

const Index = (config: any) => {
  return <ActiveRingChart config={config} style={{ width: '100%', height: '100%' }} />
}

export default Index
